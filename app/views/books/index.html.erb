
<%= render 'users/info', user: @user %>
<%= render 'books/info', book: @book %>

<h1>Books</h1>
<% @books.each do |book| %>
  
  <p>Title</p>
  <!--リンク先がedit-->
  <span><%= book.title %></span>
  <p>opinion</p>
  <span><%= book.body %></span>

<% end %>