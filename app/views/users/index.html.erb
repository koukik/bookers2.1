
<div class="container">  <!-- この行を追加 -->
  <div class="row">
    <div class="col-lg-4">
      <%= render 'users/info', user: @user %>
      <%= render 'books/info', book: @book %>
    </div>
    <div class="col-lg-8">
      <table class="table table-striped">
        <% @users.each do |user| %>
          <h1>Users</h1>
          <p>image</p>
          <p>name</p>
          <span><%= user.name %></span>
          <li>
          <%= link_to 'sow', user_path(current_user.id) %>
          </li>
        <% end %>
      </table>
    </div>
  </div>  <!-- この行を追加 -->
</div>
