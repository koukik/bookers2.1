
<h1>User info</h1>
<%= form_with model:@user, url:"/user/#{@user.id}", local:true do |f| %>
  <h4>name</h4>
  <%= f.text_field :title %>
  <h4>Image</h4>
  <%= %>
  <h4>introducion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Update User' %>
<% end %>